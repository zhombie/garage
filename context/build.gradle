android {
    defaultConfig {
        consumerProguardFiles 'consumer-rules.pro'
    }

    resourcePrefix 'context_'
}

dependencies {
    // Annotation
    implementation 'androidx.annotation:annotation:1.4.0'

    // Core
    implementation 'androidx.core:core-ktx:1.9.0'
}