apply plugin: 'kotlin-parcelize'

android {
    namespace 'kz.garage.chat'

    defaultConfig {
        consumerProguardFiles 'consumer-rules.pro'
    }

    resourcePrefix 'chat_'
}

dependencies {
    // multimedia-store
    implementation project(':multimedia:store')
}