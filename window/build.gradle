android {
    namespace 'kz.garage.window'

    defaultConfig {
        consumerProguardFiles 'consumer-rules.pro'
    }

    resourcePrefix 'window_'
}

dependencies {
    // Core
    implementation 'androidx.core:core-ktx:1.10.1'

    // Window
    api 'androidx.window:window:1.0.0'
}