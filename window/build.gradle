android {
    defaultConfig {
        consumerProguardFiles 'consumer-rules.pro'
    }

    resourcePrefix 'window_'
}

dependencies {
    // Core
    implementation 'androidx.core:core-ktx:1.9.0'

    // Window
    api 'androidx.window:window:1.0.0'
}