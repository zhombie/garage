android {
    namespace 'kz.garage.textview'

    defaultConfig {
        consumerProguardFiles 'consumer-rules.pro'
    }

    resourcePrefix 'textview_'
}

dependencies {
    // Annotation
    implementation 'androidx.annotation:annotation:1.4.0'
}